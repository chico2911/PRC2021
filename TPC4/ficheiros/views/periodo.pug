extends layout

block content
    .w3-card-4
        header.w3-container.w3-teal
            h3.w3-center= num+'º Periodo'
        .w3-container
            ul
                each elem in elems
                    li 
                        a(href='/elementos/'+elem.name)= elem.name
        footer(class="w3-container w3-teal")
            adress.w3-right A85367
            a(href="/") Voltar